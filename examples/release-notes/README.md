# Release notes

Use this configuration with `openmaintainer release-notes --from v0.1.0 --to HEAD --fixture fixtures/pull-requests/merged.json --mock --dry-run`.

Run from the checkout root or supply an absolute path to verified merged-PR metadata. Refs label output; they do not fetch changes from git or GitHub.
