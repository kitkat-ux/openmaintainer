# Repository scripts

Build tooling is kept in package scripts so contributors can use the same commands locally and in CI. This directory is reserved for small, dependency-free maintenance scripts; provider and GitHub behavior belongs in the workspace packages.
